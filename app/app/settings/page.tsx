import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Settings"
      title="Settings"
      description="Org profile, members, API keys (BYOK), GitHub, environments, data residency, data export & deletion, legal, danger zone."
      scheduledTurn="Turn 7"
    />
  );
}
