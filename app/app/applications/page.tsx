import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Applications"
      title="Applications"
      description="Every app your team scopes, builds, ships, and runs on Assembly. Tracks lifecycle from Draft → Live, with build context for Claude Code and the linked ticket history."
      scheduledTurn="Turn 3"
    />
  );
}
