import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Governance"
      title="Governance"
      description="PII detection and masking, data transformation rules, compliance rule packs, retention policy. The rules every app inherits."
      scheduledTurn="Turn 6"
    />
  );
}
