import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Agents"
      title="Agent library"
      description="Pre-built, production-ready agents. Point them at your governed data, set your rules, switch them on."
      scheduledTurn="Turn 6"
    />
  );
}
