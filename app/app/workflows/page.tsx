import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Workflows"
      title="Workflows"
      description="Durable orchestrations on Temporal. Workflow detail, run history, HITL review inbox, and a live run monitor."
      scheduledTurn="Turn 6"
    />
  );
}
