import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Releases"
      title="Releases"
      description="The production-readiness gate. Promotion queue, pre-production checklist, Org Admin approval, versions & rollback, change requests."
      scheduledTurn="Turn 4"
    />
  );
}
