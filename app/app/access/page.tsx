import { ModuleStub } from "@/components/ModuleStub";
export default function Page() {
  return (
    <ModuleStub
      eyebrow="Access — the moat"
      title="Roles, permissions, access simulator"
      description="The highest-priority module per spec. Built-in + custom roles, row- and field-level permissions, SSO, and an access simulator that previews the data plane as any user."
      scheduledTurn="Turn 3"
    />
  );
}
